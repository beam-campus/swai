defmodule Swai.Accounts.Facts do
  def user_budget_changed, do: "user_budget_changed"
  def user_created, do: "user_created"
  def user_deleted, do: "user_deleted"
  def user_confirmed, do: "user_confirmed"
end
